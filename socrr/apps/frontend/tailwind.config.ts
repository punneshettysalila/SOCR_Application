// TODO: Configure TailwindCSS design tokens and content paths for SOCRR.
const config = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
